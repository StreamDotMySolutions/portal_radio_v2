import React from 'react';
import PageContent from '../ContentLayout/components/PageContent';

const SingleArticle = ({id}) => {
    return (
       <PageContent id={id} />
    );
};

export default SingleArticle;